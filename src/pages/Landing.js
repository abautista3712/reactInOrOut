import React, { useState, useEffect } from "react";

// import Search from "./Search";

const Landing = (props) => {
  return (
    <div className="">
      <img
        src="./assets/images/original/choppingBoard.JPG"
        alt="Chopping Board with Food"
        className="backgroundImg"
      />
    </div>
  );
};

export default Landing;
